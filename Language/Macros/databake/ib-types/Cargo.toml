[package]
name = "ib-types"
version = "0.1.0"
edition = "2024"

[features]
build = ["dep:databake", "dep:serde"]

[dependencies]
databake = { version = "0.2", optional = true, features = ["derive"] }
serde = { version = "1", optional = true, features = ["derive"] }
